// Moved to lib/findings.ts (needed by more than just FindingCard now — the
// findings-popover shared component uses it too); re-exported so this file's
// existing importers don't need to change.
export { lineLabel } from "@/lib/findings";
